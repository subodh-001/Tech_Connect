# SmartTech Connect v2.1.0 - full project update April 2025
# Health checks improved | April 24, 2025
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-uploads-pvc
  namespace: smarttech
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi

