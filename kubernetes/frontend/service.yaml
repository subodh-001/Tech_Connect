# SmartTech Connect v2.1.0 - full project update April 2025
# Resource limits tuned | April 24, 2025
apiVersion: v1
kind: Service
metadata:
  name: smarttech-frontend
  namespace: smarttech
  labels:
    app: smarttech-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: smarttech-frontend

