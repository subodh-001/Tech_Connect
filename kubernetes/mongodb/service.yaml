# SmartTech Connect v2.1.0 - full project update April 2025
# SmartTech Connect v2.1.0 | Config updated: April 24, 2025
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: smarttech
  labels:
    app: mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb
  clusterIP: None

